# CLAUDE.md

This is an Obsidian knowledge vault generated from the grandMA2 User Manual
at https://help.malighting.com/grandMA2/en/help/

Generated by `scripts/extract.js` via `npm run obsidian`.

---

## Structure

```
Sections/            — ~45 top-level chapter index notes (type: section)
Pages/{Section}/     — ~350 procedure/concept notes (type: page)
Keywords/            — ~200 CLI keyword reference notes (type: keyword)
Keys/                — ~70 console key/button notes (type: key)
QuickStart/          — ~20 Quick Start Guide notes (type: quick-start)
000 Map of Content.md         — top-level index (type: moc)
000 Keyword Index.md          — alphabetical keyword index (type: moc)
000 Key Index.md              — console key index (type: moc)
```

### Section groupings

| Folder | Manual chapter |
|---|---|
| `Sections/Command Syntax and Keywords/` | Command Syntax and Keywords |
| `Sections/Presets/` | Presets |
| `Sections/Cues and Sequences/` | Cues and Sequences |
| `Sections/Executors/` | Executors |
| `Sections/Effects/` | Effects |
| `Sections/Patching/` | Patching, DMX, and Fixture Setup |
| `Sections/Basic Fixture Types/` | Basic Fixture Types |
| `Sections/Advanced Fixture Types/` | Advanced Fixture Types |
| `Sections/Macros/` | Macros |
| `Sections/MAtricks/` | MAtricks |
| `Sections/Networking/` | Networking |
| `Sections/Remote Control/` | Remote Controlling the System |
| `Sections/Timecode/` | Timecode |
| `Sections/Workspace/` | Workspace |
| `Sections/Groups/` | Groups |
| `Sections/Worlds Filters Masks/` | Worlds, Filters and Masks |
| *(others)* | All remaining manual chapters |

---

## Conventions

- ALWAYS use `[[wikilinks]]` for internal links.
- Source callout pattern: `> [!source]- Source` followed by the canonical MA help URL.
- Section nav footer pattern: `← [[prev]] | [[next]] →` then `Part of [[Section]] · [[000 Map of Content]]`.
- Do NOT create links to notes that don't exist unless asked.
- Keyword notes use backtick code format for inline CLI syntax: `` `Store Preset N /global /overwrite /noconfirm` ``

---

## Frontmatter fields

### All notes
- **type**: `section` | `page` | `keyword` | `key` | `quick-start` | `moc`
- **slug**: the original MA help slug, e.g. `key_presets_create`
- **url**: canonical source URL
- **tags**: array — `type/{type}`, `section/{slug}` for non-keyword nodes
- **aliases**: from `meta-keywords` and `meta-searchwords` in page `<head>`
- **cssclasses**: `gma2-{type}` — drives per-type reading-view styling via the
  `.obsidian/snippets/note-types.css` snippet

### `page` notes (additional)
- **section**: parent section title string
- **ma2_section**: parent section slug
- **section_ref**: wikilink to parent section note
- **prev_page**: wikilink or null
- **next_page**: wikilink or null
- **depth**: `2` (direct child) or `3` (grandchild within section)

### `keyword` notes (additional)
- **keyword**: the exact keyword string, e.g. `Store`
- **keyword_type**: `function` | `object` | `helping` | `special-char`
- **related_key**: wikilink to corresponding console key note (if exists), e.g. `[[Keys/Store]]`

### `key` notes (additional)
- **key_label**: display label of the key, e.g. `Store`
- **related_keyword**: wikilink to corresponding keyword note (if exists)

### `section` notes (additional)
- **page_count**: number of direct child pages
- **pages**: array of wikilinks to all pages in this section

---

## Known gotchas

Captured directly in the notes:
- `Fixture` shortcut = `F`, NOT `Fix` (Fix = freeze/lock)
- `/global` flag is case-sensitive — not `/Global`, not `/g`
- `List Preset` "Special=Normal" does NOT indicate Selective after `/global` store
- `List Preset N.1 Thru N.9999` enumerates pool — bare `list preset N` mis-scopes
- `SaveShow` requires `/noconfirm` in automation contexts

---

## Color system (Obsidian graph view)

| Group | Color |
|---|---|
| Keywords | `#E15759` |
| Keys | `#F28E2B` |
| Sections | `#4E79A7` |
| Pages | `#76B7B2` |
| QuickStart | `#59A14F` |
| MOC | `#F97316` |

This system is **shipped, not just documented**: `.obsidian/graph.json` defines the
six graph-view color groups (by path), and `.obsidian/snippets/note-types.css`
(auto-enabled via `.obsidian/appearance.json`) applies matching per-type accents in
reading view, keyed off each note's `cssclasses`.

---

## Stats (post-generation)

Populated by `npm run stats` after extract. Updated in README.md.

---

## Tooling (`scripts/`)

This repository is a **self-contained knowledge resource**: the scripts here only
build, link, and validate the notes — there is no analytics, graph, or retrieval
layer in the vault itself.

- `extract.js` (`npm run obsidian`) — scrape the grandMA2 manual and generate the vault.
- `convert-links.js` (`npm run links`) — rewrite `[[wikilinks]]` to relative markdown
  links for the `github-browse` branch.
- `generate-moc.js` (`npm run moc`) — regenerate the Map of Content / indexes.
- `stats.js` (`npm run stats`) — write live note counts into `README.md`.
- `migrate-internal-links.js` (`npm run migrate:links`) — one-shot: rewrite any raw
  internal `key_*.html` body links to `[[wikilinks]]`.
- `audit.js` (`npm run audit`) — read-only conformance check (unresolved wikilinks,
  raw internal links, structure / `type`-vs-location / frontmatter / convention issues).

Dev tooling: ESLint + Prettier (vault markdown excluded via `.prettierignore`),
Husky (pre-commit lint-staged, commit-msg commitlint, pre-push test+audit), all
also enforced by `ci.yml`.

---

## Branch structure

| Branch | Purpose |
|---|---|
| `main` | Source of truth — Obsidian `[[wikilinks]]` format |
| `github-browse` | Auto-generated — relative markdown links for GitHub browsing |
| `gh-pages` | Auto-generated — Quartz static site |

Both `github-browse` and `gh-pages` update automatically when `main` is pushed.

### Build notes / gotchas

- **Quartz is pinned to `v4.5.2`** in `.github/workflows/sync.yml` (gh-pages job). Quartz **v5**
  dropped the clone-and-build model this workflow uses (`quartz.config.ts` / `.quartz/plugins`
  are now scaffolded by `npx quartz create`), so a bare clone of `main` breaks `npx quartz build`.
  Do **not** un-pin without reworking the job to the `npx quartz create` flow. The workflow runs
  on **Node 22** (Quartz's engine requirement).
- **`github-browse` link conversion** (`scripts/convert-links.js`) resolves wikilinks by
  vault-root-relative path, then case-insensitively. Notes use **path-qualified** links
  (`[[Sections/Networking]]`, `[[Pages/.../Note]]`); bare links are only the root `[[000 …]]`
  index files.
- **`npm run audit`** (`scripts/audit.js`) is a read-only conformance check against this SOP:
  it reports unresolved wikilinks, **raw internal `key_*.html` links** (which 404 on
  github-browse), structure/`type`-vs-location mismatches, missing required frontmatter, and
  missing source-callout / nav-footer conventions. Run it after any extract.
  Current state: **PASS** (0 unresolved links, 0 raw internal links, 0 structure/frontmatter issues).
- **`npm run migrate:links`** (`scripts/migrate-internal-links.js`, shares
  `lib/internal-links.js` with `extract.js`/`audit.js`) is the one-shot that rewrote the raw
  `key_*.html` body links to `[[wikilinks]]`; `extract.js` now applies the same rewrite so future
  runs stay clean.
- **`npm test`** (`scripts/test/`, built-in `node --test`, no extra deps) covers the
  link-conversion, internal-link repair, slug/filename, and audit logic.
- **CI** (`.github/workflows/ci.yml`) runs `npm test` + `npm run audit` on every push to `main`
  and on PRs — the quality gate that keeps broken links / frontmatter off `main`.
- **`npm run stats` / `npm run moc`** write live note counts into `README.md` and
  `000 Map of Content.md` (no longer placeholder `—`).
